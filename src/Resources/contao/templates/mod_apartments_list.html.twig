<div class="mod_apartments_list">
    <div class="apartments-list">
        {% if apartments is empty %}
            <p class="no-apartments">Aktuell sind keine Wohnungen verfügbar.</p>
        {% else %}
            <div class="apartments-grid">
                {% for apartment in apartments %}
                    <div class="apartment-item{% if apartment.status %} status-{{ apartment.status|lower }}{% endif %}">
                        {% if apartment.bezeichnung %}
                            <h3>{{ apartment.bezeichnung }}</h3>
                        {% endif %}
                        {% if apartment.objektnummer %}
                            <span class="apartment-number">{{ apartment.objektnummer }}</span>
                        {% endif %}
                        
                        <div class="apartment-details">
                            {% if apartment.adresse %}
                                <div class="detail-row">
                                    <strong>Adresse:</strong> {{ apartment.adresse }}
                                </div>
                            {% endif %}
                            {% if apartment.bauetappe %}
                                <div class="detail-row">
                                    <strong>Bauetappe:</strong> {{ apartment.bauetappe }}
                                </div>
                            {% endif %}
                            {% if apartment.zeile %}
                                <div class="detail-row">
                                    <strong>Zeile:</strong> {{ apartment.zeile }}
                                </div>
                            {% endif %}
                            {% if apartment.etage %}
                                <div class="detail-row">
                                    <strong>Etage:</strong> {{ apartment.etage }}
                                </div>
                            {% endif %}
                            {% if apartment.zimmer %}
                                <div class="detail-row">
                                    <strong>Zimmer:</strong> {{ apartment.zimmer }}
                                </div>
                            {% endif %}
                            {% if apartment.flaeche %}
                                <div class="detail-row">
                                    <strong>Fläche:</strong> {{ apartment.flaeche }} m²
                                </div>
                            {% endif %}
                        </div>

                        {% if apartment.nettomietzins or apartment.nebenkosten or apartment.bruttomietzins %}
                            <div class="apartment-costs">
                                {% if apartment.nettomietzins %}
                                    <div class="cost-item">
                                        <span class="cost-label">Nettomiete:</span>
                                        <span class="cost-value">CHF {{ apartment.nettomietzins }}</span>
                                    </div>
                                {% endif %}
                                {% if apartment.nebenkosten %}
                                    <div class="cost-item">
                                        <span class="cost-label">Nebenkosten:</span>
                                        <span class="cost-value">CHF {{ apartment.nebenkosten }}</span>
                                    </div>
                                {% endif %}
                                {% if apartment.bruttomietzins %}
                                    <div class="cost-item total">
                                        <span class="cost-label">Bruttomiete:</span>
                                        <span class="cost-value">CHF {{ apartment.bruttomietzins }}</span>
                                    </div>
                                {% endif %}
                            </div>
                        {% endif %}

                        {% if apartment.status %}
                            <div class="apartment-status status-{{ apartment.status|lower }}">
                                {{ apartment.status }}
                            </div>
                        {% endif %}

                        <div class="apartment-actions">
                            <a href="wohnungen/details/?id={{ apartment.objektnummer_url }}" class="btn btn-details">Details ansehen</a>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>
</div>